ktor:
  application:
    modules:
      - net.kvt_craft.ApplicationKt.module
  deployment:
    port: 8080
    sslPort: 8443
  security:
    ssl:
      keyStore: keystore.jks
      keyAlias: selfsigned
      keyStorePassword: password
      privateKeyPassword: password